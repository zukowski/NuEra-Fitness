<% if @quote %>
  $("#quick-quote").html('<%= escape_javascript("Shipping: #{number_to_currency @quote}"); %>')
<% else %>
  alert("<%= @message || 'Your order requires a freight shipping quote' %>");
<% end %>
