<script type="text/javascript">
$(document).ready(function() {
	$('img.hover').hover(
	function() {
		var temp = $(this).attr('src');
		$(this).attr('src',$(this).attr('togglesrc'));
		$(this).attr('togglesrc',temp);
	},
	function() {
		var temp = $(this).attr('src');
		$(this).attr('src',$(this).attr('togglesrc'));
		$(this).attr('togglesrc',temp);
	});
});
</script>
<% cache(:social_and_search) do %>
  <%= render 'shared/social_and_search' %>
<% end %>
<ul class="nav-bar">
  <% if current_order && current_order.line_items.any? %>
    <%= render 'shared/store_menu' %>
  <% else %>
    <% cache("store-menu-#{current_user ? 1 : 0}") do %>
      <%= render 'shared/store_menu' %>
    <% end %>
  <% end %>
</ul>
