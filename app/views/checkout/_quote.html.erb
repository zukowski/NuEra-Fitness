<fieldset id="quote_details">
  <div class="clear"></div>
  <legend><%= t(:quote) %></legend>
  <%= render :partial => 'shared/quote_details', :locals => {:order => @order} %>
</fieldset>
<br />
<div class="form-buttons">
  <input type="submit" class="continue button primary" value="<%= t :get_quote %>" />
</div>
